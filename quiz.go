// We need to have a package name on top of every go file
// This is the main package
// This is the entry point of the application
// We can have only one main package in a project
// We can have multiple packages in a project
// We can have multiple files in a package
// We can have multiple functions in a file
package main
